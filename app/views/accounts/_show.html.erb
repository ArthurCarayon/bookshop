<% if current_user && current_user.avatar.present? %>
    <% image_tag @user.avatar.variant(resize_to_limit: [200, 200]) %>
<% end %>

<% link_to "Editer mes infos", edit_account_path(@user) %>
<% link_to "Delete", account_path(@user), method: :delete, data:{ turbo_method: :delete, turbo_confirm: "Your account is deleted" } %>

